# 크리마 리뷰 대시보드 — 담당자 PC 이전 설치 가이드

이 문서는 "슬룸 리뷰 대시보드" 데이터 업로드·관리 업무를 새 PC(담당자)로 넘길 때 필요한
설치 순서와 매달 해야 할 작업을 정리한 것입니다. 순서대로 따라 하면 됩니다.

대시보드 자체의 일상적인 사용법(Taxonomy 분류, AI 기능, 협업 등)은 같은 폴더의
**`대시보드_사용가이드.md`** 를 참고하세요. 이 문서는 "PC 세팅"에 집중합니다.

---

## ⚠️ 이 압축파일에는 원본 리뷰 CSV(개인정보 포함)가 들어있습니다

`data/raw/` 폴더에 지금까지의 원본 리뷰 CSV(회원명·주문번호 등 개인정보 포함)가
그대로 들어있습니다. 이메일/메신저로 전달할 때 암호화하거나, 사내 보안 정책에 맞는
방법(비밀번호 압축, 사내 파일 공유 등)으로 전달하세요. 이 CSV들은 **절대 GitHub에
올라가지 않습니다**(`.gitignore`로 커밋 차단돼 있음 — 대시보드에는 익명화된 버전만 올라감).

---

## 0. 먼저 알아둘 것 — 이 프로젝트의 AI는 전부 Claude Code CLI를 씁니다

이 프로젝트는 **AI 처리를 전부 Claude(구독 인증)로 진행**합니다. 그래픽카드(GPU)나
로컬 AI 서버(Ollama 등)는 전혀 필요 없습니다 — Node.js + Claude Code CLI 로그인만
되어 있으면 어떤 PC에서든 동일하게 동작합니다.

이 프로젝트의 AI는 크게 두 갈래이며, 둘 다 Claude를 씁니다.

1. **월간 데이터 처리 파이프라인** (`update-data.bat`) — CSV 업로드할 때 한 달에 한 번 도는 것
2. **대시보드 라이브 AI 기능** (`start-ai-local.bat`) — 대시보드 열어놓고 "AI 요약",
   "🤖 AI 자동 분류" 같은 버튼을 그때그때 누르는 것

Claude로 처리하면 다음 특징이 있습니다:
- **세션 사용량 한도가 있습니다.** 한도에 도달하면 자동으로 리셋 시각까지 대기했다가
  **스스로 다시 이어서 진행**합니다(사람이 지켜보거나 재실행할 필요 없음).
- **1번(월간 파이프라인)은 몇 시간~하루 정도 걸릴 수 있습니다.** 창을 그대로 켜두고
  퇴근하거나 다른 일을 하시면 됩니다. (컴퓨터가 잠자기 모드로 안 들어가게만 해주세요 —
  Windows 설정에서 "전원 및 절전"의 절전 시간을 늘려두면 좋습니다.)
- 중간에 실수로 창을 닫아도, `update-data.bat`을 다시 실행하면 **이미 끝난 부분은 건너뛰고**
  이어서 진행합니다(진행 상황이 자동 저장됨).
- **1번과 2번은 같은 Claude 계정 한도를 나눠 씁니다.** 대시보드에서 AI 버튼을 많이 누르면
  그만큼 월간 처리용 한도가 줄어듭니다.
- **각자 자기 PC에서 실행해야 각자의 계정이 쓰입니다** — 자세한 건 4번 참고.

---

## 1. 미리 설치해야 하는 프로그램

아래 4가지만 설치하면 됩니다. 순서대로 진행하세요.

### 1) Python (3.10 이상)
- https://www.python.org/downloads/ 에서 최신 버전 다운로드
- 설치 시 **"Add python.exe to PATH"** 체크박스 반드시 체크

### 2) Git
- https://git-scm.com/download/win 에서 다운로드 후 기본 옵션으로 설치

### 3) Node.js (Claude Code CLI 설치에 필요)
- https://nodejs.org 에서 LTS 버전 다운로드 후 설치

### 4) Claude Code CLI
설치 후 PowerShell(또는 명령 프롬프트)에서:
```
npm install -g @anthropic-ai/claude-code
claude
```
`claude` 를 처음 실행하면 브라우저가 열리며 로그인을 요청합니다. **회사 Claude 계정으로
로그인**하세요(이 작업은 사람이 직접 브라우저에서 해야 하며, 자동화할 수 없습니다).
로그인 후 아래처럼 나오면 정상입니다:
```
claude -p "핑" --model sonnet
```
→ 응답이 오면 준비 완료.

---

## 2. 프로젝트 압축 풀기 + 설치

1. 전달받은 `crema-review-dashboard-이전패키지.zip` 을 원하는 위치에 압축 해제
   (예: `C:\work\crema-review-dashboard`)
2. 압축을 푼 폴더를 열고, 주소창에 `cmd` 입력 후 Enter (그 폴더에서 명령 프롬프트가 열립니다)
3. 아래 명령 실행 (한 번만 하면 됨):
   ```
   pip install -r requirements.txt
   ```
4. GitHub 접근 권한 확인 — 이 프로젝트는 `https://github.com/Lyrze/crema-review-dashboard` 에
   결과를 올립니다. **이 GitHub 저장소에 쓰기(push) 권한이 있는 계정으로 로그인**돼 있어야
   마지막 업로드 단계가 됩니다. (저장소 소유자에게 GitHub 아이디를 알려주고 협업자로
   Write 이상 권한으로 추가해달라고 요청하세요 — 초대 메일/알림을 수락해야 실제로 적용됩니다.
   최초 1회 `git push` 실행 시 GitHub 로그인 창이 뜨면 로그인하면 됩니다.)

---

## 3. 매달 하는 일 — 월간 데이터 처리 (실제 업무 루틴)

### 1) 새 리뷰 CSV 넣기
크리마(리뷰 수집 서비스)에서 받은 이번 달 리뷰 CSV 파일을 아래 위치에 넣습니다:
```
data\raw\{브랜드명}\{YYYY-MM}\reviews.csv
```
예: `data\raw\슬룸\2026-06\reviews.csv`
(폴더가 없으면 새로 만들면 됩니다. 브랜드명은 한글 그대로, 파일명은 항상 `reviews.csv`.
지금까지의 2026-03~05월 원본 CSV는 이미 이 위치에 들어있습니다.)

### 2) update-data.bat 더블클릭 실행
1. 처리할 브랜드/월을 번호로 선택하면, 이후 모든 AI 분석 단계(감성 정밀화·키워드 재분류·
   PVOC 감성판정 등)가 **자동으로 전부 포함되어** 진행됩니다(따로 선택할 것 없음).
2. 창이 계속 진행되며 로그가 출력됩니다. **닫지 말고 그대로 두세요.**
   - 중간에 "한도 소진 — N분 대기 후 재시도" 같은 메시지가 나오는 건 정상입니다.
     자동으로 기다렸다가 다시 진행합니다.
3. 모든 단계가 끝나면 자동으로 GitHub에 업로드되고 완료 메시지가 뜹니다.
4. 대시보드 확인: https://lyrze.github.io/crema-review-dashboard/
   (업로드 후 1~2분 뒤 반영됩니다)

### 3) 중간에 창을 닫았다면?
`update-data.bat`을 다시 실행하고 **같은 브랜드/월을 다시 선택**하면, 이미 끝난 부분은
건너뛰고 이어서 진행합니다. 처음부터 다시 하지 않아도 됩니다.

---

## 4. 대시보드 라이브 AI 기능 쓰기 (선택 — "AI 요약"/"AI 자동 분류" 버튼들)

대시보드를 열어놓고 그 자리에서 "AI 요약", "AI 분석", Taxonomy의 "🤖 AI 자동 분류" 같은
버튼을 누르는 기능들은 위 월간 처리와 별개로, 사이드바 하단 **"AI 서버 URL"**에 연결된
서버가 필요합니다.

### 실행 방법 — `start-ai-local.bat` (권장, 본인 계정 전용)

1. 압축 푼 폴더에서 **`start-ai-local.bat`** 더블클릭
2. "AI 백엔드 : Claude Code CLI ... 로그인됨" 로그가 뜨면 준비 완료
3. 대시보드(https://lyrze.github.io/crema-review-dashboard/) 좌측 사이드바 하단
   **"AI 서버 URL"** 입력란에 `http://localhost:8799` 입력 → Enter
4. 이제 AI 버튼들이 정상 작동합니다. 만약 Claude 로그인이 안 돼 있으면 버튼을 처음 눌렀을 때
   **자동으로 브라우저 로그인 창이 뜹니다** — 로그인 완료 후 버튼을 다시 누르면 됩니다.

**⚠️ 왜 "로컬(localhost)"만 쓰나요?** `start-ai-local.bat`은 터널을 쓰지 않고, 이 창을 실행한
**이 PC에 로그인된 Claude 계정만** 사용합니다. 다른 사람과 공유되는 구조가 아니라서,
**팀원은 각자 자기 PC에서 이 파일을 직접 실행**하면 자동으로 각자의 계정이 쓰입니다.
(참고: `start-tunnel.bat`으로 터널을 열어 그 URL을 팀원에게 공유하면, 그 팀원도 터널을 켠
사람의 Claude 계정/한도를 같이 쓰게 됩니다 — 팀원과 공유하는 용도로 쓰지 마세요. 터널은
"내가 다른 기기(폰 등)에서 내 PC로 접속"하는 용도로만 권장합니다.)

**주의**: `start-ai-local.bat` 창을 닫으면 이 라이브 AI 기능들이 즉시 멈춥니다(월간 처리와는
무관 — 월간 처리는 `update-data.bat`이 따로 도는 것이라 영향 없습니다). 자리를 비울 땐
최소화만 하고 그대로 두세요.

---

## 5. 문제가 생겼을 때

| 증상 | 확인할 것 |
|---|---|
| `claude` 명령이 안 먹힘 | Node.js/Claude Code CLI 재설치, 새 명령 프롬프트 창에서 재시도 |
| "한도 소진" 메시지가 계속 반복됨 | 정상 동작입니다. 자동으로 대기 후 재시도하니 기다리면 됩니다. 하루 넘게 안 끝나면 문의 |
| `git push` 실패 ("Push 실패") | GitHub 계정 로그인 상태 확인, 저장소 협업자 권한(Write 이상) 확인 |
| 데이터 처리 중 오류로 멈춤 | 화면에 뜬 `[ERROR]` 문구를 캡처해서 문의 |
| 대시보드에 반영이 안 됨 | GitHub Actions 배포는 1~2분 소요. 그래도 안 되면 저장소의 Actions 탭 확인 |
| "AI 요약" 버튼 눌러도 반응 없음 | `start-ai-local.bat`이 켜져 있는지, 사이드바 "AI 서버 URL"이 `http://localhost:8799`인지 확인 |

더 자세한 기술 문서는 프로젝트 루트의 `CLAUDE.md` 파일을 참고하세요(개발자용이지만
문제 상황과 원인이 상세히 기록돼 있습니다).

---

## 6. 이 패키지에 포함된 것 / 안 된 것

- ✅ 전체 소스코드, 스크립트, 지금까지 처리된 월별 데이터(요약·통계), git 이력
- ✅ **원본 리뷰 CSV(`data/raw/`, 2026-03~05월)도 포함되어 있습니다** — 개인정보(회원명·
  주문번호 등)가 담겨 있으니 전달·보관 시 위 "⚠️" 안내대로 취급하세요. 이 폴더는
  `.gitignore`로 막혀 있어 GitHub에는 절대 올라가지 않습니다.
- ✅ `대시보드_사용가이드.md` — 대시보드 일상 사용법(Taxonomy·AI·협업) 안내
- ❌ GitHub 접속 토큰 등 인증 정보는 포함되지 않았습니다 — 새 PC에서 각자 로그인하세요.
