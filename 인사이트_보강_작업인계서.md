# 🔄 인사이트(complaint/improvement) 보강 작업 인계서

> **작성일**: 2026-08-27 (업데이트: 같은 날 — §1의 complaint 재검증 5개월 전체 완료·배포됨.
> §1은 이제 참고용 기록으로만 보고, §3(사용자 추가 확인 요청 2건)부터 이어서 진행할 것.)
> **목적**: 이 세션에서 하던 작업을 다른 사람이 이어받을 수 있도록 현재 상태와 남은 작업을 정리.
> **핵심 배경**: `docs/data/슬룸/{월}/keywords.json`의 `by_intent.complaint`/`improvement`가
> 리뷰 수 대비 과소 집계되던 문제(원인 여러 개)를 찾아 `scripts/process_data.py` 패턴을
> 계속 보강해왔고, 매번 `rebuild_keywords.py`(정규식 재추출) → `reverify_suspect.py`(Claude
> AI 3단계 게이트로 오탐 제거) 순으로 반영해왔음. 관련 선행 문서: `개선인사이트_보강_지시서.md`
> (다운로드 폴더에 있던 원본 지시서, 이번 작업의 출발점).

---

## 1. 지금 당장 확인해야 할 것 — 백그라운드 작업

**세션 종료 시점에 아래 명령이 백그라운드로 돌고 있었을 수 있음** (또는 중간에 죽었을 수 있음 — 먼저 확인):

```bash
python scripts/quota_retry.py -- python scripts/reverify_suspect.py --brand 슬룸 --months 2026-03,2026-04,2026-05,2026-06,2026-07 --polarities complaint
```

로그 파일: `_pipeline_complaint2.log` (프로젝트 루트). 실행 중인지 프로세스 확인:

```bash
# Windows PowerShell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match "reverify_suspect" } | Select-Object ProcessId, CreationDate, CommandLine
```

**완료 여부 확인**:
```bash
python3 -c "
import json
for m in ['2026-03','2026-04','2026-05','2026-06','2026-07']:
    d = json.load(open(f'docs/data/슬룸/{m}/keywords.json', encoding='utf-8'))
    items = d['by_intent']['complaint']
    v = sum(1 for x in items if x.get('ai_reclassified'))
    print(m, 'complaint', f'{v}/{len(items)}')
"
```

**세션 종료 시점 진행 상태** (참고용, 실제 확인 필수):
- 2026-03: ✅ 완료 (14/14)
- 2026-04: ✅ 완료 (13/13)
- 2026-05: ❌ 미완료 (0/12)
- 2026-06: ❌ 미완료 (0/14)
- 2026-07: ❌ 미완료 (0/20)

**만약 죽어있으면(프로세스 없음, 진행 정체)** — 아래로 재시작:
```bash
claude -p "1+1"   # CLI 살아있는지 먼저 확인 (2가 나와야 함)
python scripts/quota_retry.py -- python scripts/reverify_suspect.py --brand 슬룸 --months 2026-03,2026-04,2026-05,2026-06,2026-07 --polarities complaint
```
(`--polarities complaint`만 지정 — improvement/praise는 이미 5개월 전체 완료돼 있어서 건드릴 필요 없음)

### ⚠️ 반복적으로 겪은 문제: "org's monthly spend limit"
- 세션 한도(quota)와 다른, **조직 전체의 Claude API 월간 지출 한도**. 짧으면 몇 분, 길면 5시간 이상 안 풀린 적도 있음(예측 불가).
- `scripts/claude_engine.py`의 `is_quota()`와 `scripts/reverify_suspect.py`의 로컬 사본 둘 다 이미 이 문구를 인식하도록 고쳐놔서(2026-08-25 작업), `quota_retry.py`가 30분 간격으로 **자동 재시도**함 — 사람이 "다시 시도"라고 말 안 해도 됨. 그냥 두면 됨.
- 다만 확실한 완료 시각은 예측 불가하니, 이 인계서를 남기는 것.

---

## 2. 지금까지 확정 완료 + 배포된 것 (git log 참고)

| 커밋 | 내용 |
|---|---|
| `4f4eb1e`, `f989bdf` | COMPLAINT/IMPROVEMENT_PATTERNS 신규 다수 추가(벨크로/고정, 사이즈, 재질, 커버/세탁, 냄새 등) + Taxonomy 🧩제품경험 신규 Topic 3개 |
| `834a6ce` | 인사이트 카드 그룹핑을 `category` ID 기준 명시적 매핑(`IM_CATEGORY_GROUP`)으로 재설계 — 기존엔 라벨 문자열에 느슨한 정규식 매칭이라 "개선" 한 글자 때문에 다른 그룹이 다 삼켜지던 버그 있었음(수정 완료, 배포됨) |
| `582cc3d` | "고장" 활용형 대량 누락 수정, "환불반품"/"AS문제" 안↔않 보강, "파손" 신규, `rebuild_keywords.py` `--only` 옵션 + 완료마커 자동 무효화 버그 수정 |

### ⚠️ 아직 커밋 안 된 것 (로컬에만 있음, 반드시 다음 배포에 포함)
```
scripts/process_data.py                         (수정됨, 미커밋)
docs/data/슬룸/2026-03~07/keywords.json 5개       (수정됨, 미커밋 — 백그라운드 재검증이 지금 이 파일들을 쓰는 중)
```

**미커밋 상태로 남은 이유(가장 중요한 변경)**: complaint 매칭 풀을 **"별점≤3"에서 "별점≤3 OR 감성=negative"로 확장**함.

- **발견 경위**: 목마사지베개V2(2026-07) 인사이트 카드에서 "부정리뷰 78건인데 왜 키워드는 32건만 잡히나"는 질문에 답하다가, ★4~5인데 감성은 negative인 리뷰(예: "★5인데 충전기가 고장나여 ㅠㅠ")가 **애초에 매칭 후보 풀에도 안 들어가는** 구조적 문제를 발견.
- **규모**: 전체 브랜드 기준 부정감성 리뷰 822건 중 **239건(29%)**이 이 이유로 complaint 패턴 매칭 대상에서 통째로 제외되고 있었음.
- **성격**: `개선인사이트_보강_지시서.md`가 지적했던 "improvement 풀이 부정감성만 대상"이던 문제의 **거울상 버그**(반대로 complaint는 별점만 보고 감성을 안 봄).
- **코드 위치**: `scripts/process_data.py`의 `extract_keywords_basic()` 함수, `complaint_pool` 변수(예전엔 `low_df` 그대로 썼음).
- **검증**: 7월 기준 새로 잡힌 35건 전부 스팟체크 — 전부 진짜 불만(오탐 없음).
- **효과**: 5개월 complaint 총 매칭이 대략 2배 가까이 늘어남(예: 06월 47→77건, 07월 60→128건).

**할 일**: 1번의 재검증이 끝나면(complaint 5개월 전부 `ai_reclassified` 완료) →
```bash
git add scripts/process_data.py "docs/data/슬룸/2026-03/keywords.json" "docs/data/슬룸/2026-04/keywords.json" "docs/data/슬룸/2026-05/keywords.json" "docs/data/슬룸/2026-06/keywords.json" "docs/data/슬룸/2026-07/keywords.json"
git commit -m "fix: complaint 매칭 풀을 별점≤3 OR 감성=negative로 확장 (거울상 버그 수정)"
git push origin main
```

---

## 3. 사용자가 제기한 추가 확인 요청 (미해결, 조사 필요)

### 3-1. "목마사지베개V2 개선요청이 진짜 15개가 맞아?"

현재(2026-07) 데이터 기준 실측 확인함:
```
효과/성능 강화 요청 2건, 기능 추가 요청 4건, 강도/세기 조절 3건,
조작/리모컨 개선 3건, 커버/세탁 개선 2건, 무선/충전 개선 1건 = 합계 15건
(전 리뷰 427건 중)
```
전부 `ai_reclassified: true`(AI 검증 통과분)라 **지금 로직 기준으로는 15건이 맞음**. 다만 사용자가 "너무 적은 것 같다"고 의심하는 이유가 합리적임 — 427건 리뷰 중 15건(3.5%)이 낮아 보일 수 있음.

**조사 방향**:
1. `discover_keywords.py`가 이미 이 달(2026-07) 미포착 개선요청 후보를 클러스터링해둔 게 있음(`docs/data/슬룸/2026-07/keyword_candidates.json`) — 목마사지베개V2 관련 후보만 걸러서 실제로 더 있는지 확인.
   - 이전 세션에서 확인했던 후보 예: "사이즈/착용감 개선요청 (10건)", "밴드 길이 부족" 등 — 이게 목마사지베개V2 얘긴지 다른 제품(팔꿈치마사지기 등) 얘긴지 상품별로 나눠서 재확인 필요(당시엔 브랜드 전체 기준으로만 봄, 상품별 분리 안 함).
2. `_imp()` 게이트(주제어+결핍/요망신호어 ±8자 인접)가 너무 엄격해서 놓치는 표현이 있는지 — 2번 항목(§4)의 발견과 연결.
3. IMPROVEMENT_PATTERNS 자체가 10개뿐이라(기능추가/강도/무선충전/소음/조작/크기/AS반품/경량화/효과강화/내구성/고정/커버세탁/냄새), 목마사지베개V2 특유의 요청(예: 사이즈 옵션, 좌우구분 등 — discover 후보에서 나왔던 것들)이 정식 패턴이 아직 없어서 샐 수 있음.

### 3-2. "긍부정 기준이 별점이랑 감성이랑 똑같다"

**확인한 사실** (`docs/index.html`):
- `senClass(r)` 함수(6817번 줄)가 전역 `SENT_MODE`(`'sentiment'` 또는 `'rating'`, 우측 상단 토글로 전환·localStorage 저장)에 따라 분기함. `sentiment` 모드면 AI 감성필드, `rating` 모드면 `rating>=4?긍정:rating<=2?부정:중립`.
- 이 함수는 섹션 ⑥⑦⑧(상품VOC/구매경험VOC/제품경험)의 긍/부정 막대에 쓰임 — **토글하면 이 섹션들은 실제로 달라져야 정상**(§2에서 확인한 239건 불일치가 있으므로).
- **BUT 인사이트 카드(PRAISE/COMPLAINTS/ENHANCEMENT/SUMMARY)와 `keywords.json`/`products.json`의 `positive_rate`/`negative_rate`는 `SENT_MODE`를 전혀 안 봄** — 파이프라인이 생성 시점에 고정한 `sentiment` 필드로 이미 확정된 값이라, 토글을 아무리 눌러도 안 바뀜.

**사용자가 말한 "똑같다"가 정확히 어떤 화면/비교를 가리키는지 파악 못 함** — 다음 중 하나로 추정되나 미확인:
- (a) SENT_MODE 토글을 눌러도 섹션⑥⑦⑧ 숫자가 실제로는 안 바뀌는 버그일 가능성 (있어서는 안 되는데, 실제로 239건 불일치가 있으니 토글 시 숫자가 달라져야 정상 — 실제로 안 달라진다면 버그)
- (b) 인사이트 카드(SENT_MODE 무관, 항상 감성 기준)와 섹션⑥⑦⑧(토글 가능)을 같은 화면에서 비교하다가 "왜 여긴 안 바뀌지?"라고 느꼈을 가능성(이건 설계상 정상이지만 사용자에게 안 알려줘서 혼란)

**할 일**: 실제 대시보드 열어서 SENT_MODE 토글(우측 상단 "감성기준"/"별점기준" 칩) 눌러보면서 섹션⑥⑦⑧ 숫자가 실제로 바뀌는지 라이브로 재현 확인. 안 바뀌면 진짜 버그(코드 위치: `docs/index.html:6816` `SENT_MODE` 정의, `6850`번 줄 근처 토글 핸들러, 각 섹션의 `renderProductVOC()`/`renderPVOC()` 등이 `senClass`/`senPos`를 실제로 다시 호출해서 리렌더하는지 확인).

---

## 4. 참고 — 이번 세션에서 발견했던 한국어 정규식 함정 (앞으로도 반복될 수 있는 패턴)

새 패턴/신호어 추가할 때마다 아래 유형의 실수가 반복됐음. 다음에 패턴 건드릴 때 체크리스트로 활용:

1. **모음축약 활용형 누락**: "크다"→"커요/커서/큰것/컸다", "아프다"→"아파요/아파서" — 어간 규칙활용/불규칙활용으로 원형 글자 자체가 안 남는 경우가 많음. `.*크` 같은 패턴은 반드시 활용형까지 나열(`크|커|큰|컸`) 필요.
2. **"안" vs "않" 부정 표현**: "안 되다"(전치부정)와 "~지 않다"(후치부정)는 다른 글자. `(안|않)` 둘 다 커버해야 함.
3. **홑글자/짧은 단어의 오탐 폭발**: "개선"/"좋"/"선"/"안" 처럼 흔한 글자를 단독으로 패턴에 넣으면 의도치 않은 문맥까지 다 잡음(예: "고장없이"의 "고장", "어렵지 않고"의 "않"). 반드시 실측 데이터로 오탐률 스팟체크 후 배포.
4. **문서화된 회귀 버그**: `rebuild_keywords.py`로 특정 polarity를 재계산하면 `.reverify_progress.json`의 `__done_pol__`을 같이 무효화해야 함(2026-08-26에 이걸 놓쳐서 재검증이 통째로 스킵되는 버그 겪음 — `582cc3d`에서 수정 완료, 이제 자동 처리됨).

---

## 5. 체크리스트 요약

- [ ] 백그라운드 `reverify_suspect.py --polarities complaint` 완료 확인(§1) — 죽어있으면 재시작
- [ ] 완료되면 `process_data.py` + 5개월 `keywords.json` 커밋·푸시(§2)
- [ ] 목마사지베개V2 개선요청 15건이 진짜 전부인지 discover_keywords 후보로 교차검증(§3-1)
- [ ] SENT_MODE 토글이 섹션⑥⑦⑧에서 실제로 작동하는지 라이브 재현(§3-2)
- [ ] 배포 후 라이브 사이트에서 인사이트 카드 재확인(사용자가 최근 2번 "그룹핑이 이상하다"고 했다가 둘 다 브라우저 캐시 문제로 판명 — 재현 안 되면 강제 새로고침 먼저 요청)
