# 크리마 리뷰 대시보드 — 사용 가이드 

> 대시보드 주소: **https://lyrze.github.io/crema-review-dashboard/**
> 링크만 열면 바로 사용할 수 있습니다. 설치할 프로그램은 없고, 인터넷 브라우저(크롬 권장)만 있으면 됩니다.

이 문서는 **① Taxonomy(분류) 사용법 / ② AI 사용법 / ③ 분류 데이터 저장·불러오기** 세 가지를 다룹니다.
처음이시면 위에서부터 천천히 읽어 주세요.

---

## 들어가기 전에 — 화면 구조 한눈에 보기

- **왼쪽 메뉴(사이드바)**: 분석 섹션으로 이동하는 목록입니다. 아래쪽에 `Taxonomy`, `제품 경험`, `AI 모델` 설정이 있습니다.
- **가운데(메인 화면)**: 리뷰 지표, SKU 분석, 구매경험 VOC, 제품 경험 등 분석 결과가 나옵니다.
- **숫자 섹션 순서**: ① Executive Summary → … → ⑦ 구매경험 VOC → ⑧ 제품 경험 → ⑨ 커스텀 차트

> 💡 화면이 이상하거나 옛날 내용이 보이면 **Ctrl + Shift + R**(강력 새로고침)을 한 번 눌러 주세요. 대부분 해결됩니다.

---

## 1. Taxonomy(분류) 사용법

### 1-1. Taxonomy가 뭔가요?
리뷰를 **주제(Topic)별로 묶어 보는 분류 도구**입니다.
예를 들어 "효과 좋아요", "시원해요" 같은 리뷰는 **효과/효능** 주제로, "배송 빨라요"는 **배송** 주제로 자동으로 묶입니다.

분류는 **키워드 규칙**으로 자동 작동합니다. 즉 어떤 주제에 "시원, 개운, 풀려" 같은 키워드가 등록돼 있으면, 그 단어가 들어간 리뷰가 자동으로 그 주제에 모입니다.

### 1-2. 들어가는 법
왼쪽 메뉴에서 **📂 Taxonomy** 를 클릭하면 분류 관리 화면이 열립니다.

기본으로 두 종류의 분류가 미리 만들어져 있습니다(지우셔도 다음에 열면 다시 생깁니다):
- **🛒 구매경험** — "어떻게 알고 샀나 / 왜 샀나 / 사고 나서 무슨 일이 있었나" 관점 (광고, 추천, 배송, 반품 등)
- **🧩 제품 경험** — "제품 자체가 어떤가" 관점 (효과, 강도, 온열, 소음, 디자인, 가격 등)

### 1-3. 분류 안을 살펴보기
분류 카드를 클릭하면 상세 화면이 열립니다.
- **왼쪽 목록**: 그 분류의 주제(Topic)들과, 맨 위의 **📭 미분류** 항목
- 주제를 클릭하면 **오른쪽에 그 주제로 묶인 리뷰**가 보입니다.
- **📭 미분류** = 아직 어떤 주제에도 안 잡힌 리뷰입니다. (※ 다른 분류에 이미 들어간 리뷰는 미분류에서 빠지므로 중복으로 세지 않습니다.)

### 1-4. 분류를 더 정확하게 만드는 3가지 방법

**(A) 키워드 규칙 직접 편집** — 가장 기본
1. 주제를 선택 → **✎ 규칙 편집** 클릭
2. 키워드를 쉼표로 구분해 추가/삭제 (예: `시원, 개운, 풀려`)
3. 저장하면 그 단어가 든 리뷰가 즉시 그 주제로 모입니다.

**(B) 미분류 리뷰를 손으로 분류** — 자동으로 안 잡히는 리뷰 처리
1. 왼쪽 **📭 미분류** 클릭
2. 분류하고 싶은 리뷰의 **체크박스** 선택
3. 아래 바에서 **대상 Topic 선택 → "이 Topic으로 분류"** 클릭

**(C) ✦ 키워드 학습** — (B)를 한 뒤 효과를 영구적으로 만들기 ⭐ 중요
- 분류 상세 화면 **맨 위 "✦ 키워드 학습 (수동 분류 → 규칙)"** 버튼을 누르면,
  손으로 분류한 리뷰들에서 **공통 키워드를 자동으로 뽑아** 각 주제 규칙에 추가해 줍니다.
- **왜 중요한가?** 분류는 키워드로 작동하므로, 한 번 학습해 키워드를 넣어두면
  **다음 달 새 리뷰 데이터에도 자동으로 같은 분류가 적용**됩니다. (= 일이 쌓일수록 더 똑똑해짐)
- 👉 정리하면: **손으로 분류(B) → 꼭 "키워드 학습"(C)까지 눌러 주세요.** 그래야 그 노력이 다음 달에도 이어집니다.

> 손으로 분류한 "이 리뷰 = 이 주제" 기록 자체는 그 달 리뷰에만 적용됩니다(리뷰 번호가 달마다 달라서요).
> 다음 달까지 이어지는 건 **키워드**뿐입니다. 그래서 (C) 키워드 학습이 핵심입니다.

---

## 2. AI 사용법

### 2-1. AI로 뭘 할 수 있나요?
- **Hey Sloom (AI 챗)**: 화면 데이터에 대해 자유롭게 질문 → AI가 요약/분석해 답변
- **AI 분석 버튼**: 각 섹션의 데이터를 AI가 해석해 인사이트 제공
- **✦ 키워드 학습 / AI 자동 그룹화**: Taxonomy에서 리뷰를 읽고 키워드·주제를 제안

### 2-2. ⚠️ AI를 쓰려면 'AI 서버'가 켜져 있어야 합니다
이 대시보드의 AI 버튼들(Hey Sloom, AI 분석 등)은 외부 유료 서버가 아니라, **각자 자기 PC에서
직접 켜는 AI 서버**를 씁니다. Claude Code CLI(구독 인증)로 동작하며 GPU가 필요 없습니다.

1. 프로젝트 폴더에서 **`start-ai-local.bat`** 더블클릭
2. 창에 "AI 백엔드 : Claude Code CLI ... 로그인됨" 이 뜨면 준비 완료
   (로그인이 안 돼 있으면 잠시 후 대시보드에서 AI 버튼을 처음 누를 때 **자동으로 브라우저
   로그인 창**이 뜹니다 — 로그인만 하면 됩니다)
3. 왼쪽 사이드바 아래 **"AI 서버 URL"** 칸에 `http://localhost:8799` 입력 → Enter
4. 이제 AI 버튼들이 작동합니다

> ⚠️ **반드시 자기 PC에서 직접 실행하세요.** 이 창을 실행한 PC에 로그인된 Claude 계정을
> 그대로 씁니다 — 팀원 각자 자기 PC에서 `start-ai-local.bat`을 실행해야 각자의 계정이
> 쓰입니다. 이 창을 닫으면 AI 기능이 멈춥니다(자리 비울 땐 최소화만 하세요).

**다른 기기(예: 내 폰)에서 내 PC의 AI를 쓰고 싶다면**: `start-tunnel.bat`을 대신 실행하면
터널 주소(URL)가 발급됩니다. 그 주소를 다른 기기의 사이드바 "AI 서버 URL"에 붙여넣으면
됩니다. 단, 이 URL도 결국 터널을 켠 PC의 Claude 계정을 그대로 쓰므로 **팀원과 공유하지
마세요**(공유하면 그 팀원도 당신의 계정/한도를 쓰게 됩니다) — "내가 다른 기기에서 내 PC로
접속"하는 용도로만 쓰세요. 로컬로 되돌리려면 **↺ 로컬** 클릭.

연결 상태는 사이드바의 **동그란 점**으로 표시됩니다 (초록=연결됨 / 회색=꺼짐).
**AI 모델** 칸에서 사용할 모델을 고를 수 있습니다(보통 기본값 그대로 두면 됩니다).

> 💡 AI 서버가 꺼져 있어도 **대시보드와 분류 기능은 정상 작동**합니다. AI가 필요한 버튼만 잠시 안 될 뿐입니다.
> 예외로 **✦ 키워드 학습**은 AI가 꺼져 있어도 작동합니다(이 경우 자동으로 '빈도 기반'으로 키워드를 뽑습니다 — 품질은 AI보다 조금 낮음).

### 2-3. 팁
- AI 답변은 시간이 조금 걸릴 수 있습니다(수십 초). 기다려 주세요.
- 답이 이상하면 질문을 더 구체적으로 바꿔서 다시 물어보세요.

---

## 3. 분류 데이터 저장 / 불러오기

분류 작업 결과(키워드, 손으로 한 분류 등)는 아래 방식으로 보관·공유됩니다.
Taxonomy 화면 오른쪽 위 버튼들이 이 기능입니다.

### 3-1. 자동 저장 (기본)
- 분류를 바꾸면 **내 브라우저에 자동 저장**됩니다. 따로 저장 버튼을 누를 필요 없습니다.
- ⚠️ 단, **이 브라우저(이 PC)에만** 저장됩니다. 다른 사람 화면엔 자동으로 안 보입니다.
  → 여러 명이 함께 작업하려면 아래 **🌐 실시간**을 켜세요.

### 3-2. 🌐 실시간 — 팀 실시간 공유 (협업할 때 강력 추천)
- 오른쪽 위 **🌐 실시간** 버튼을 클릭해 **🟢 실시간 ON** 으로 바꾸면,
  내가 한 분류가 **클라우드에 자동 저장**되고 **다른 담당자 화면에도 실시간 반영**됩니다.
- **두 사람 모두 🟢 ON** 이어야 서로 보입니다. (한 명이라도 OFF면 그 사람은 자기 PC에만 저장)
- 한 번 켜두면 다음에 열 때 자동으로 연결됩니다.
- 두 명이 서로 다른 주제에 키워드를 추가하면 **둘 다 보존**됩니다(자동 합쳐짐).

> 즉, **다른 PC·다른 담당자가 작업해도 양쪽 다 실시간 ON이면 문제없이 합쳐집니다.**
> 같은 항목을 동시에 고치면 나중에 저장한 내용이 반영됩니다(큰 정리는 한 명이 할 때 하는 걸 권장).

### 3-3. ☁️ 저장소 업로드 / 불러오기 — 수동 백업·이전
특정 시점 결과를 저장소(GitHub)에 남기고 싶을 때 씁니다.
> 담당자에게 URL 요청이 필요합니다.

- **☁️ 저장소 업로드**: 현재 분류 결과를 저장소에 올립니다. (담당자 이름·날짜가 태그로 붙음)
  - ※ 업로드는 담당 PC의 연결(터널)이 켜져 있어야 합니다.
- **☁️ 불러오기**: 저장소에 올라온 분류본 목록에서 골라 불러옵니다. (누구나 가능)

## 빠른 작업 순서 (요약)

협업으로 분류 품질을 올리는 표준 흐름입니다:

1. 대시보드 링크 열기 → **Ctrl+Shift+R** 한 번
2. **📂 Taxonomy → 🌐 실시간 ON** (팀과 같은 분류 공유)
3. 분류 카드 열기 → **📭 미분류** 에서 리뷰를 주제에 손으로 분류 (체크 → "이 Topic으로 분류")
4. 분류 상세 맨 위 **✦ 키워드 학습** 눌러서 키워드 규칙에 반영 (→ 다음 달도 자동 적용)
5. 필요하면 **✎ 규칙 편집** 으로 키워드 직접 다듬기

> 막히는 부분이 있으면 캡처해서 알려 주세요. 화면 명칭(버튼 글자) 그대로 말해 주시면 빠르게 도와드릴 수 있습니다.
