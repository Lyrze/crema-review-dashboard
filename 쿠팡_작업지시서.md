# 🛒 쿠팡 리뷰 월간 업로드 작업 지시서

> 슬룸 브랜드의 **쿠팡 채널 리뷰**를 매월 자사몰·스마트스토어 데이터에 **합쳐서** 대시보드에 올리는 절차입니다.
> 쿠팡 리뷰는 대시보드에서 **빨간색 "쿠팡" 뱃지**로 구분됩니다.
> **자사몰·스마트스토어 데이터는 절대 건드리지 않고, 쿠팡만 추가(cp_ 접두사)** 합니다.

이미 **2026-03 ~ 2026-07** 은 반영 완료. 이 문서는 **다음 달(예: 2026-08)부터** 이어서 하는 방법입니다.

---

## 0. 사전 준비 (최초 1회)

- **Python 3.10+**, **Git**, **Claude Code CLI** 로그인 (자사몰 파이프라인과 동일 환경)
- 쿠팡 감성은 **별점 기반**이라 Claude 사용이 적지만, **본문 있는 리뷰의 별점↔내용 불일치 교정**에만 Claude를 씁니다 → 로그인은 되어 있어야 함

---

## 1. GitHub 최신 데이터 받기 (매번 먼저!)

다른 작업자(자사몰·스마트스토어)가 올린 최신본을 먼저 받아야 충돌이 안 납니다.
```bash
cd <저장소 폴더>            # crema-review-dashboard
git checkout main
git pull origin main
```
> ⚠️ 로컬에 작업 중이던 게 있어 충돌하면, 커밋 안 된 변경은 정리(또는 stash) 후 pull 하세요.
> `git status` 로 깨끗한지 확인하고 시작하는 게 안전합니다.

---

## 2. 자사몰 해당 월이 먼저 올라와 있어야 함 ⚠️

쿠팡은 **기존 슬룸 월에 얹는** 방식이라, 그 달의 **자사몰(슬룸) 데이터가 대시보드에 먼저 있어야** 합니다.
- `docs/data/슬룸/{처리월}/reviews.json` 이 있는지 확인 (예: `docs/data/슬룸/2026-08/`)
- 없으면 → **자사몰 담당자가 그 달 `update-data.bat` 처리·업로드를 먼저 끝낸 뒤** 진행하세요.
  (자사몰 → 스마트스토어 → 쿠팡 순서로 얹는 게 원칙)

---

## 3. 쿠팡 리뷰 파일 준비

1. 쿠팡에서 **제품별 리뷰 CSV**를 내려받아 한 폴더에 모읍니다. (파일명 = 상품명, 예: `허리편한케어 V1.csv`)
   - 컬럼 형식: `No. / ReviewId / Stars / Date/Author/Shop / Option Name / Review Content / Image URLs / Video URLs`
   - 내보내기에 과거 리뷰가 다 들어있어도 됩니다 — 변환기가 **처리월만** 골라냅니다.
2. 그 폴더를 저장소 안에 둡니다. 예: `쿠팡 리뷰 분류 필요/` (이 폴더는 자동으로 git 무시됨 — 커밋 안 됨)

---

## 4. 변환 → 분류 → 병합 (처리월 = 예시 2026-08)

아래 3줄을 순서대로. **`2026-08` 자리에 실제 처리월**을 넣으세요.

```bash
# (1) 변환: 쿠팡 CSV → 자사몰 스키마, 해당 월만, cp_ 접두사, 별점감성, 별점만→안내문
python scripts/convert_coupang.py --indir "쿠팡 리뷰 분류 필요" --months 2026-08

# (2) 분류: 별점 감성 + '본문 있는 리뷰만' Claude가 별점↔내용 불일치 교정 (별점만은 별점감성 유지)
python scripts/run_coupang_classify.py --months 2026-08

# (3) 병합: 슬룸 2026-08 에 쿠팡 주입 (자사몰·스마트스토어 미변경, 추가만)
python scripts/merge_smartstore.py --ss-brand 쿠팡 --prefix cp_ --channel 쿠팡 --months 2026-08
```

> 여러 달을 한 번에 하려면 쉼표로: `--months 2026-08,2026-09`
> (2)번은 Claude 세션 한도에 걸리면 자동 대기·재개됩니다(quota_retry). 중단돼도 다시 실행하면 이어받음.

---

## 5. 검증 (배포 전 필수)

```bash
python scripts/validate_data.py --brand 슬룸 --month 2026-08
```
- **✅ PASS** 면 다음 단계로.
- **❌ FAIL(감성 커버리지)** 이면 → 그 달 슬룸 리뷰 중 감성 없는 게 있는 것. 보통 자사몰 쪽 문제이니 자사몰 담당자와 확인. (쿠팡 리뷰는 별점감성이 100% 채워짐)

---

## 6. 업로드 (커밋 & 푸시)

```bash
git add docs/data/슬룸/ docs/index.html
git status                     # 슬룸 {월} 데이터 변경만 있는지 확인
git commit -m "data: 슬룸에 쿠팡 채널 병합 (2026-08)"
git push origin main
```
- push 거부되면 **force-push 금지**. `git pull origin main` → (충돌 시 해결) → 다시 push.
- `docs/data/쿠팡/` 폴더와 `data/raw/쿠팡_변환/` 는 **중간 파일**이라 커밋 안 함(대시보드엔 별도 브랜드로 안 뜸). 병합 후 지워도 됩니다.

---

## 7. 확인
- 대시보드(https://lyrze.github.io/crema-review-dashboard/) 접속 → 해당 월 선택 → 리뷰 원문 화면에서
  쿠팡 리뷰에 **빨간 "쿠팡" 뱃지**가 보이면 성공. (1~2분 뒤 반영)

---

## 규칙 요약 / 주의
- **별점 기반 감성**: ★4~5 긍정 / ★3 중립 / ★1~2 부정. 단, 본문 있는 리뷰는 Claude가 내용 보고 불일치만 교정(별점 높은데 부정 내용 등).
- **별점만 남긴 리뷰**: 본문에 "별점만 남기고 별도 리뷰작성하지 않음" 자동 입력, 별점감성 그대로(내용 체크 안 함).
- **cp_ 접두사** = 자사몰(숫자)·스마트스토어(ss_)와 충돌 안 나게 + 뱃지 표시 근거.
- **자사몰·스마트스토어 미변경**: 병합은 기존 리뷰를 그대로 두고 쿠팡만 더합니다. 멱등(같은 달 두 번 병합해도 중복 안 됨).
- **상품명 매핑**: 쿠팡 상품명(파일명)이 자사몰과 표기가 다르면 별도 상품으로 잡힐 수 있음 → 대시보드 상품 매핑에서 검토.
- **PVOC(구매경험 VOC 감성기반)** 에는 쿠팡이 안 들어갑니다(별점 기반 + 별점만 다수라 의도적으로 제외). 정상입니다.

---

## 스크립트 요약
| 스크립트 | 역할 |
|---|---|
| `scripts/convert_coupang.py` | 쿠팡 제품별 CSV → 자사몰 스키마(월 필터·cp_·별점감성·별점만 처리) |
| `scripts/run_coupang_classify.py` | 별점 감성 + 본문만 Claude 불일치 교정 |
| `scripts/merge_smartstore.py --prefix cp_ --channel 쿠팡` | 슬룸에 쿠팡 주입(스마트스토어 병합과 동일 도구) |
| `scripts/validate_data.py` | 배포 전 정합성 검증 |

*작성 기준: 2026-08. 최초 2026-03~07 반영 완료본 이후의 월간 반복 절차.*
