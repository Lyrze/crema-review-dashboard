# 🧩 분리된 상품명 통합 안내 (담당자용)

> 채널(자사몰·스마트스토어·쿠팡)을 합치다 보면 **같은 상품인데 이름이 조금 달라 대시보드에 따로**
> 잡히는 경우가 있습니다. (예: 쿠팡 `목베개플러스` vs 자사몰 `목베개 플러스`)
> 이 문서는 그걸 **하나로 합치는 방법**과 **매월 반복 절차**입니다.

---

## 1. 왜 분리되나

- **쿠팡 상품명 = 업로드한 CSV 파일명**을 그대로 씁니다. → 자사몰 정식명과 **띄어쓰기·표기**가 다르면 다른 상품으로 잡힘.
- 자사몰은 옵션 기반 매핑(`product_mapping.json`)으로 정식명이 정리돼 있지만, 쿠팡은 그 매핑을 안 거칩니다.

## 2. 이번에 통합한 4건 (2026-03~07)

| 분리돼 있던 이름 | → 통합한 이름 |
|---|---|
| `목마사지베개 V2` (쿠팡) | `목 마사지 베개 V2` |
| `목베개플러스` (쿠팡) | `목베개 플러스` |
| `바디풀고컷` (쿠팡) | `바디 풀고컷` |
| `프리미엄 엘보케어 팔꿈치 마사지기` (자사몰) | `팔꿈치 마사지기` |

이 매핑은 **`scripts/product_consolidate.json`** 에 저장돼 있습니다.

---

## 3. 매월 반복 절차 (쿠팡 병합 뒤에 1번 실행)

쿠팡 병합(`쿠팡_작업지시서.md` 4번) **직후**, 아래를 실행하면 위 매핑이 자동 적용됩니다:

```bash
python scripts/consolidate_product.py --brand 슬룸 --months 2026-08
```
- `--map` 안 주면 `scripts/product_consolidate.json` 의 매핑을 자동으로 씁니다.
- 이미 통합된 이름이면 그대로 두고, 분리된 것만 합칩니다(감성값은 안 건드림, `.bak` 백업 생성).
- 그 다음 평소대로 `validate_data.py` → `git add` → `commit` → `push`.

> 즉 매월 순서: **자사몰 처리 → 쿠팡 변환·분류·병합 → `consolidate_product.py` → 검증 → 업로드**

---

## 4. 새로 분리된 상품이 생겼을 때 (새 매핑 추가)

새 달 데이터에서 또 분리가 보이면, **분리된 이름과 합칠 이름을 찾아** config에 추가하면 됩니다.

### (1) 분리 후보 찾기
아래를 실행하면 **쿠팡에만 있는 상품**(자사몰과 이름이 안 맞아 분리 의심)을 보여줍니다:
```bash
python - <<'PY'
import json
from collections import defaultdict
pc=defaultdict(lambda: defaultdict(int))
for m in ["2026-08"]:                      # ← 확인할 월
    r=json.load(open(f"docs/data/슬룸/{m}/reviews.json",encoding="utf-8"))["reviews"]
    for k,v in r.items():
        ch="cp" if str(k).startswith("cp_") else ("ss" if str(k).startswith("ss_") else "jm")
        pc[v.get("product","")][ch]+=1
print("[쿠팡에만 있는 상품 = 분리 의심]")
for p,c in sorted(pc.items()):
    if c.get("cp") and not c.get("jm") and not c.get("ss"):
        print(f"  {p!r}  (cp {c['cp']})")
print("[자사몰/스마트만 있는 상품 = 위와 짝일 수 있음]")
for p,c in sorted(pc.items()):
    if not c.get("cp") and (c.get("jm") or c.get("ss")):
        print(f"  {p!r}  (jm {c.get('jm',0)} ss {c.get('ss',0)})")
PY
```
목록에서 **같은 상품인 짝**을 찾습니다(대개 쿠팡 이름 ↔ 자사몰 이름).

### (2) config에 추가
`scripts/product_consolidate.json` 의 `map` 에 `"분리된이름": "합칠이름"` 추가:
```json
{
  "map": {
    "목마사지베개 V2": "목 마사지 베개 V2",
    "새로운 쿠팡이름": "자사몰 정식명"
  }
}
```
> 합칠 이름은 보통 **리뷰가 더 많은 쪽(자사몰 정식명)** 으로. (팔꿈치처럼 특별히 짧은 이름을 원하면 그쪽으로)

### (3) 다시 실행
```bash
python scripts/consolidate_product.py --brand 슬룸 --months 2026-08
```

---

## 5. 주의
- **감성·별점은 안 바뀝니다** — 이름/집계(상품 카드·키워드 상품분포)만 합칩니다.
- **멱등** — 같은 달 두 번 돌려도 이미 합쳐진 건 그대로.
- 특정 건만 임시로 합치려면 config 대신 직접 지정:
  `python scripts/consolidate_product.py --brand 슬룸 --months 2026-08 --map "A=>B"`
- 통합 후엔 반드시 `validate_data.py` PASS 확인 후 push.

---

*관련 문서: `쿠팡_작업지시서.md`(쿠팡 월간 업로드 전체 절차). 이번 통합분은 2026-03~07 반영 완료.*
